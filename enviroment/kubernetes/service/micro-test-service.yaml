apiVersion: v1
kind: Service
metadata:
  name: micro-test
  namespace: micro-test-ns
spec:
  selector:
    app: micro-test
  ports:
  - protocol: TCP
    port: 4000
    targetPort: 80
  type: NodePort